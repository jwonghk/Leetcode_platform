// For tailwind verson 4.1x : config must have these plugins format in order for 
// tailwind to generate proper css file for the custom color, etc within the tailwind.config.js file
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
